/**
 * Per-environment configuration.
 * Set apiOrigin to the deployed Laravel backend origin (no trailing slash).
 * Ignored when the page is served from localhost/127.0.0.1 (local dev uses :8000).
 */
window.APP_CONFIG = {
  apiOrigin: ''
};
